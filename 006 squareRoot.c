#include<stdio.h>
#include<math.h>

int main() {
	double number1;
	printf("");
	scanf("%lf",&number1);
	
	double squareRoot = sqrt(number1); // Compute the square root with sqrt()
	printf("%.2lf",squareRoot); t()
	return 0;
	
}